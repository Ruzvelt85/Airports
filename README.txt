Service AirportsApi allows to calculate distance between two airports in miles.

The service implements as a GET http-request with a signature:
{api-url}/airports/getdistanceinmiles?airportCode1={iata-code}&airportCode={iata-code}. 
